pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Build triggered automatically!'
            }
        }

        stage('Test') {
            steps {
                echo 'Running tests...'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying application...'
            }
        }
    }
}